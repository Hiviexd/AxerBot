import { MessageEmbed } from "discord.js";

export default new MessageEmbed({
	title: "huh?",
	description: "Command not found! check `!help` for a list of available commands.",
	color: "#ea6112",
});